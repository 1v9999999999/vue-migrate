{
  "name": "@vue-migrate/plugin-vue3-types",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "P3 TypeScript type-completion plugin: infer types from data()/props in Vue2 Options API and add equivalent type annotations (return-type on data, JSDoc/TS on props). Marks this.$refs/$store/$route as TODO.",
  "dependencies": {
    "@vue-migrate/core": "workspace:*",
    "@babel/traverse": "^7.25.0",
    "@babel/types": "^7.25.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
