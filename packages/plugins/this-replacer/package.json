{
  "name": "@vue-migrate/plugin-this-replacer",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Detect Vue2 prototype-injected properties (this.$http / this.$axios / this.$fetch / this.$api / this.$util / etc.) and either auto-rewrite to a discovered import or mark for manual review.",
  "dependencies": {
    "@vue-migrate/core": "workspace:*",
    "@babel/traverse": "^7.25.0",
    "@babel/types": "^7.25.0",
    "@babel/parser": "^7.25.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
