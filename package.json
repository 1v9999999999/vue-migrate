{
  "name": "vue-migrate",
  "version": "0.1.0",
  "private": true,
  "description": "Vue2 to Vue3 (and more) automated migration tool — plugin-based pipeline architecture",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @vue-migrate/cli dev",
    "dev:cli": "pnpm --filter @vue-migrate/cli dev",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@11.15.1",
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "node-cron": "^4.6.0"
  }
}
